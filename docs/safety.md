# 文件安全

0.1 版本不实现真实文件扫描和操作。

`FileExecutor` 的移动、重命名和删除方法全部抛出
`FeatureDisabledError`。后续实现必须满足：

- 仅访问用户授权目录。
- 默认拒绝 Windows 系统目录。
- 不跟随符号链接和目录联接。
- AI 只能返回内部文件 ID 与目标桶 ID。
- 操作前重新验证文件状态。
- 所有操作先预览并由用户确认。
- 删除进入回收站。
- 写入可恢复的操作日志。

