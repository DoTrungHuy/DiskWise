# 隐私设计

- 本地 Ollama 是默认 Provider。
- 云端 API 默认关闭。
- 云端调用必须携带显式用户授权状态。
- 本地调用失败不会自动切换到云端。
- API 密钥不进入 SQLite、日志、Git 或错误消息。
- 后续发送云端前应移除绝对路径、用户名和不必要的文档内容。
- 后续图片上传必须单独获得用户授权。

0.1 版本尚未读取用户文件，因此不会向任何模型发送用户文件内容。

